function paperFold() {
  let arr = [];
  for (let i = 1; i <= 100; i++) {
    let one = 1;
    let zero = 0;
    arr.push(one);
    arr.push(zero);
  }
  console.log(arr);
}

paperFold();
